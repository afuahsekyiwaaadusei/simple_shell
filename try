#include <unistd.h>
#include <stdio.h>
#include "main.h"

/**
 * main - used to test which function
 *
 * Return: always 0.
 */
/*int main(void)
{
	char *path = which("ls");

	printf("%s\n", path);
	return (0);
}*/


/**
 *main - used to test strcmp function
 *
 *Return: always 0.
 */

/*int main(void)
{
	int i = _strcmp("env", "exit");
	printf ("%d\n", i);
	return (0);
}*/


/**
 *main - used to test strcat function.
 *
 *Return: always 0;
 */

int main(void)
{
	char *str = _strcat("simple", "shell");

	printf("%s\n", str);
	return (0);
}






/**
 * main - used to test how the shell.c behaves when space is passed to it
 *
 * Return: always 0.
 */
/*int main(void)
{
	char *line = NULL, *dup_str, **argv;
	size_t n = 0;
	ssize_t nread;
	
	nread = getline(&line, &n, stdin);
	if (line[nread - 1] == '\n')
                        line[nread - 1] = '\0';
	printf("%ld\n", nread);
	fflush(stdout);
	dup_str = str_dup(line);
	printf("dup_str is %s\n", dup_str);
	fflush(stdout);
	argv = _argv(dup_str);
	printf("seg fault not caused by argv\n");
	fflush(stdout);
	if (execve(argv[0], argv, NULL) == -1)
		perror(argv[0]);
}*/
